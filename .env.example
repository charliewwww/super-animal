# Optional. The app falls back to browser speech + comfort replies when this is down.
EXPO_PUBLIC_VOICE_URL=http://127.0.0.1:8765

# Voice server: mock works without a GPU. Use gpu only on the home PC.
VOICE_MODE=mock
TTS_ENGINE=cosyvoice2
VOICE_PORT=8765
